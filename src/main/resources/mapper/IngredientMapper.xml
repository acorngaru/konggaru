<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acorngaru.konggaru.mapper.IngredientPageMapper">

    <select id="searchIngredient" parameterType="hashmap" resultType="Ingredient">
        select * from (
                          SELECT ROW_NUMBER() OVER (ORDER BY  id ASC) NUM, A.*
                          FROM  ingredient A
                          WHERE name LIKE '%' || #{name} || '%'
                          ORDER BY id ASC
                      )
        where NUM BETWEEN #{first} and #{last}
    </select>

    <select id="countIngredientByName" parameterType="String" resultType="int">
        select count (*)
        from ingredient
        where name LIKE '%'|| #{name} || '%'
    </select>

    <select id="allIngredient" resultType="Ingredient">
        select * from ingredient order by id
    </select>

    <select id="selectAllOrder" resultType="Ingredient_Order">
        select * from ingredient_order order by ingredient_id
    </select>


    <delete id="ingredientDel" parameterType="int">
        delete
        from Ingredient
        where id = #{num}
    </delete>

    <update id="updateIngredient" parameterType="Ingredient">
        update ingredient
        set name =#{name} , quantity=#{quantity}, price=#{price},
            unit = #{unit}, partner =#{partner}
        where id =#{id}

    </update>

    <insert id="create" parameterType="Ingredient">
        <selectKey keyProperty="id" resultType="int" order="BEFORE">
            select ingredient_seq.nextval from dual
        </selectKey>
        insert into ingredient (id,name,quantity,price ,unit,partner)
        values (#{id},#{name},#{quantity},#{price},#{unit},#{partner})

    </insert>

    <delete id="ingredientDelAll" parameterType="arraylist">
        delete from Ingredient where id in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <insert id="orderAddIngredient" parameterType="Ingredient_Order">
        insert into ingredient_order (ingredient_id,ingredient_name,ingredient_quantity,created_at,addmission_at)
                    values (#{ingredient_id},#{ingredient_name},#{ingredient_quantity},#{created_at},#{addmission_at})
    </insert>

    <update id="orderCreatedAt" parameterType="map">
        update ingredient_order set created_at = #{created_at} where ingredient_id = #{ingredient_id}
    </update>

    <update id="addMissionAt" parameterType="string">
        update ingredient_order set addmission_at = #{addmission_at}
    </update>

    <update id="updateIngredientAdd" parameterType="map">
        update ingredient set quantity =(#{quantity}+quantity) where name = #{name}
    </update>

    <delete id="ingredientOrderDel">
        delete from ingredient_order
    </delete>

</mapper>